import { MddCaseList } from "@/components/mdd/MddCaseList";

export const metadata = {
  title: "My Decision Desk",
  description: "Decision Preparation System — Phase 1",
};

export default function MddPage() {
  return <MddCaseList />;
}
